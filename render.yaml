services:
  - type: web
    name: ytvox-spleeter
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    plan: free
    envVars:
      - key: PORT
        value: 10000